name: solidui_simple_example
description: A simple example demonstrating SolidScaffold usage.
publish_to: 'none'
version: 0.0.1+0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^8.3.0
  solidui:
    path: ../
  window_manager: ^0.4.3

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
